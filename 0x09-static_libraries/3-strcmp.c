#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcmp - Copy string.
 *@s1: Hold string.
 *@s2: Hold second string.
 * Return: Results.
 */
int _strcmp(char *s1, char *s2)
{
	int myResults = strcmp(s1, s2);

	return (myResults);
}
