#include <stdio.h>
#include <ctype.h>

/**
 * _islower - check lowerxase
 * @c: int
 * Return: 0 and 1
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
